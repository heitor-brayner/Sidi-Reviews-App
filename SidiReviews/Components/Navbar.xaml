<?xml version="1.0" encoding="utf-8"?>
<UserControl 
    x:Class="SidiReviews.Components.Navbar"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:SidiReviews.Components"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">
    <UserControl.Resources>
        <SolidColorBrush x:Key="PointerOverBackgroundBrush" Color="Gray"/>
        <SolidColorBrush x:Key="PressedBackgroundBrush" Color="#7A7CFF"/>
        <SolidColorBrush x:Key="DisabledBackgroundBrush" Color="LightGray"/>
        
        <Style x:Key="NavbarButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"></Setter>
            <Setter Property="Foreground" Value="LightGray"></Setter>
            <Setter Property="FontSize" Value="18"></Setter>
            <Setter Property="FontFamily" Value="Segoe UI"></Setter>
            <Setter Property="FontWeight" Value="SemiBold"></Setter>
            <Setter Property="Padding" Value="12 6 12 6"></Setter>
            <Setter Property="CornerRadius" Value="10"></Setter>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Grid>
                            <Border x:Name="ButtonBackground" Background="{TemplateBinding Background}"
                                    CornerRadius="{TemplateBinding CornerRadius}"/>
                            <ContentPresenter Content="{TemplateBinding Content}" ContentTemplate="{TemplateBinding ContentTemplate}"
                                              HorizontalAlignment="Center" VerticalAlignment="Center" Margin="10"/>
                            <VisualStateManager.VisualStateGroups>
                                <VisualStateGroup x:Name="CommonStates">
                                    <VisualState x:Name="Normal"/>
                                    <VisualState x:Name="PointerOver">
                                        <Storyboard>
                                            <ObjectAnimationUsingKeyFrames Storyboard.TargetName="ButtonBackground"
                                                                       Storyboard.TargetProperty="Background">
                                                <DiscreteObjectKeyFrame KeyTime="0" Value="{StaticResource PointerOverBackgroundBrush}"/>
                                            </ObjectAnimationUsingKeyFrames>
                                        </Storyboard>
                                    </VisualState>
                                    <VisualState x:Name="Pressed">
                                        <Storyboard>
                                            <ObjectAnimationUsingKeyFrames Storyboard.TargetName="ButtonBackground"
                                                                       Storyboard.TargetProperty="Background">
                                                <DiscreteObjectKeyFrame KeyTime="0" Value="{StaticResource PressedBackgroundBrush}"/>
                                            </ObjectAnimationUsingKeyFrames>

                                        </Storyboard>
                                    </VisualState>
                                    <VisualState x:Name="Disabled">
                                        <Storyboard>
                                            <ObjectAnimationUsingKeyFrames Storyboard.TargetName="ButtonBackground"
                                                                       Storyboard.TargetProperty="Background">
                                                <DiscreteObjectKeyFrame KeyTime="0" Value="{StaticResource DisabledBackgroundBrush}"/>
                                            </ObjectAnimationUsingKeyFrames>

                                        </Storyboard>
                                    </VisualState>

                                </VisualStateGroup>
                            </VisualStateManager.VisualStateGroups>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="100"></RowDefinition>
        </Grid.RowDefinitions>
        <Grid Background="#0e0b3a">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="0.2*"></ColumnDefinition>
                <ColumnDefinition Width="0.2*"></ColumnDefinition>
                <ColumnDefinition Width="0.2*"></ColumnDefinition>
                <ColumnDefinition Width="0.2*"></ColumnDefinition>
                <ColumnDefinition Width="0.2*"></ColumnDefinition>
                <ColumnDefinition Width="0.2*"></ColumnDefinition>
            </Grid.ColumnDefinitions>
            <Image Grid.Column="0"
                   Source="/Assets/Logo - Branco e verde.png"
                   MaxWidth="120"
                   ></Image>
            <Button Style="{StaticResource NavbarButtonStyle}"
                    Command="{Binding NavigateCommand}"
                    CommandParameter="HomePage"
                    Grid.Column="1"
                    Content="HOME"/>
            <Button Style="{StaticResource NavbarButtonStyle}"
                    Command="{Binding NavigateCommand}"
                    CommandParameter="MoviesPage"
                    Grid.Column="2"
                    Content="FILMES"/>
            <Button Style="{StaticResource NavbarButtonStyle}"
                    Command="{Binding NavigateCommand}"
                    CommandParameter="ReviewsPage"
                    Grid.Column="3"
                    Content="REVIEWS"/>
            <Button Style="{StaticResource NavbarButtonStyle}"
                    Command="{Binding NavigateCommand}"
                    CommandParameter="MyReviewsPage"
                    Grid.Column="4"
                    Content="MINHAS REVIEWS"/>
            <Button Grid.Column="5"
                   Style="{StaticResource NavbarButtonStyle}"
                    Click="Logout_Button_Click"
                    HorizontalAlignment="Center"
                    Content="SAIR"/>
        </Grid>
    </Grid>
</UserControl>