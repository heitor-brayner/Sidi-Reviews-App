<Page x:Class="SidiReviews.Views.HomePage"
xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:local="using:SidiReviews.Views"
xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:components="using:SidiReviews.Components"
      mc:Ignorable="d"
      Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
    
    <Grid>
        
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <components:Navbar Grid.Row="0"/>
        
        <Grid Grid.Row="1" Grid.RowSpan="2">
            
            <Grid.Background>
                <LinearGradientBrush StartPoint="0,0" EndPoint="0, 1">
                    <GradientStop Color="#040e1a" Offset="0"/>
                    <GradientStop Color="#10477f" Offset="0.5"/>
                    <GradientStop Color="#040e1a" Offset="1"/>
                </LinearGradientBrush>
            </Grid.Background>
        
        </Grid>
        
        <TextBlock Grid.Row="1"
                   HorizontalAlignment="Center"
                   VerticalAlignment="Center"
                   FontSize="30"
                   Foreground="White"
                   Margin="0, 60, 0, 30">
            
            <Run Text="Bem vindo (a) ao SidiFlix,"/> <Run Text="{Binding Username}"/>
        
        </TextBlock>
        
        <Grid Grid.Row="2">
            
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Button Grid.Column="0"
                    VerticalAlignment="Center"
                    Click="PreviousButton_Click"
                    Margin="40,0,0,0"
                    Background="Transparent"
                    BorderBrush="Transparent">
                
                <FontIcon Glyph="&#xE76B;"
                          Foreground="White"
                          FontSize="24"/>
            </Button>

            <Grid x:Name="ImageGrid" Grid.Column="1">
                
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                
                <Image x:Name="Image1"
                       Grid.Column="0"
                       MaxHeight="300"
                       Margin="5"
                       Tapped="Image1_Tapped"/>
                
                <Image x:Name="Image2"
                       Grid.Column="1"
                       MaxHeight="300"
                       Margin="5"
                       Tapped="Image1_Tapped"/>
                
                <Image x:Name="Image3"
                       Grid.Column="2"
                       MaxHeight="300"
                       Margin="5"
                       Tapped="Image1_Tapped"/>
           
                <Image x:Name="Image4"
                       Grid.Column="3"
                       MaxHeight="300"
                       Margin="5"
                       Tapped="Image1_Tapped"/>
            </Grid>

            <Button Grid.Column="2"
                    VerticalAlignment="Center"
                    Click="NextButton_Click"
                    Margin="0,0,40,0"
                    Background="Transparent"
                    BorderBrush="Transparent">
                
                <FontIcon Glyph="&#xE76C;"
                          Foreground="White"
                          FontSize="24"/>
            </Button>

            <StackPanel Grid.Column="1"
                        Grid.Row="1"
                        Orientation="Horizontal"
                        HorizontalAlignment="Center"
                        Margin="0,30,0,40">
                
                <ItemsRepeater x:Name="CarouselIndicator">
                    
                    <ItemsRepeater.ItemTemplate>
                        <DataTemplate>
                            <Ellipse Width="10"
                                     Height="10"
                                     Margin="5,0"
                                     Fill="{Binding}" />
                        </DataTemplate>
                    </ItemsRepeater.ItemTemplate>
                    
                    <ItemsRepeater.Layout>
                        <StackLayout Orientation="Horizontal" Spacing="5" />
                    </ItemsRepeater.Layout>
                
                </ItemsRepeater>
            </StackPanel>
        </Grid>
    </Grid>
</Page>