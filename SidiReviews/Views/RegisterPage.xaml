<Page
    x:Class="SidiReviews.Views.RegisterPage"
xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:SidiReviews.Views"
xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:components="using:SidiReviews.Components"
    mc:Ignorable="d">

    <components:AuthPageLayout>
        <Grid Background="#fafafa">
            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" Padding="20" Spacing="10" MaxWidth="400">
                <TextBlock TextAlignment="Center" FontSize="25" FontWeight="SemiBold" Margin="0, 0, 0, 30" Text="Crie sua conta"/>
                <TextBlock FontWeight="Bold" TextAlignment="Left" Margin="0, 0, 0, 5" Text="E-mail"/>
                <TextBox Text="{Binding Email, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" PlaceholderText="Insira seu email" InputScope="EmailSmtpAddress" HorizontalAlignment="Stretch"/>
                <TextBlock FontWeight="Bold" TextAlignment="Left" Margin="0, 15, 0, 5">Usuário</TextBlock>
                <TextBox Text="{Binding Username, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" PlaceholderText="Crie um username" HorizontalAlignment="Stretch"/>
                <TextBlock FontWeight="Bold" TextAlignment="Left" Margin="0, 15, 0, 5">Senha</TextBlock>
                <PasswordBox Password="{Binding Password, Mode=TwoWay}" PlaceholderText="Crie uma senha" HorizontalAlignment="Stretch"/>
                <TextBlock Text="{Binding ErrorMessage}" Foreground="Red" FontSize="12" FontWeight="SemiBold" TextWrapping="Wrap" Margin="0,10,0,0" HorizontalAlignment="Center"/>
                <Button Command="{Binding RegisterCommand}" Background="#013dfd" Foreground="White" FontWeight="Bold" HorizontalAlignment="Stretch" Margin="0, 30, 0, 0" Content="Registrar"/>
                <HyperlinkButton Content="Já possui uma conta? Faça o login" Click="HyperlinkButton_Click_Login" HorizontalAlignment="Center" Margin="0, 10, 0, 0"/>

            </StackPanel>
        </Grid>
    </components:AuthPageLayout>

</Page>