<Page
    x:Class="SidiReviews.Views.MoviesPage"
xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:SidiReviews.Views"
xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:components="using:SidiReviews.Components"
    xmlns:model="using:SidiReviews.Model"
    xmlns:viewmodel="using:SidiReviews.ViewModel"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
    
    <Grid Background="#28313b">

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <components:Navbar Grid.Row="0"/>

        <Grid Grid.Row="1">

            <Grid.Background>
                <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                    <GradientStop Color="#040e1a" Offset="0"/>
                    <GradientStop Color="#10477f" Offset="0.5"/>
                    <GradientStop Color="#040e1a" Offset="1"/>
                </LinearGradientBrush>
            </Grid.Background>

            <GridView x:Name="MoviesGridView"
                      ItemsSource="{Binding Movies}"
                      SelectedItem="{Binding SelectedMovie, Mode=TwoWay}"
                      SelectionMode="Single"
                      IsItemClickEnabled="True"
                      Padding="24"
                      Width="1100"
                      HorizontalAlignment="Center"
                      VerticalAlignment="Stretch"
                      Visibility="Visible"
                      ScrollViewer.HorizontalScrollBarVisibility="Auto"
                      ScrollViewer.VerticalScrollMode="Enabled"
                      ScrollViewer.IsVerticalRailEnabled="True">

                <GridView.ItemsPanel>

                    <ItemsPanelTemplate>
                        <ItemsWrapGrid Orientation="Horizontal"
                                       HorizontalAlignment="Center"
                                       ItemWidth="200"
                                       Margin="0,0,-10,-10"/>
                    </ItemsPanelTemplate>

                </GridView.ItemsPanel>

                <GridView.ItemTemplate>

                    <DataTemplate>

                        <Grid Width="180" Height="300" Margin="10" Background="#1E2D3D"
                              CornerRadius="4" BorderThickness="1" BorderBrush="#2E4060">

                            <Grid.RowDefinitions>
                                <RowDefinition Height="*"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>

                            <Image Grid.Row="0" Stretch="UniformToFill" VerticalAlignment="Top" Margin="4">
                                <Image.Source>
                                    <BitmapImage UriSource="{Binding ImagePath}" DecodePixelWidth="172" />
                                </Image.Source>
                            </Image>

                            <StackPanel Grid.Row="1" Orientation="Vertical" Padding="10" Spacing="4">

                                <TextBlock Text="{Binding Title}"
                                           FontSize="14"
                                           FontWeight="SemiBold"
                                           Foreground="White"
                                           TextTrimming="CharacterEllipsis"
                                           MaxLines="2"
                                           Width="160"
                                           HorizontalAlignment="Stretch"
                                           ToolTipService.ToolTip="{Binding Title}"/>

                                <TextBlock FontSize="12" Opacity="0.8" Foreground="#CCCCCC">
                                    <Run Text="Lançamento:"/> <Run Text="{Binding ReleaseDate.Year}"/>
                                </TextBlock>

                            </StackPanel>

                            <Border Grid.RowSpan="2" BorderThickness="2" CornerRadius="{Binding Path=CornerRadius, RelativeSource={RelativeSource TemplatedParent}}"
                                    BorderBrush="#0078D4"
                                    Opacity="0">

                                <Border.Background>
                                    <SolidColorBrush Color="#0078D4" Opacity="0.1"/>
                                </Border.Background>

                                <VisualStateManager.VisualStateGroups>
                                    <VisualStateGroup x:Name="CommonStates">
                                        <VisualState x:Name="Normal" />
                                        <VisualState x:Name="PointerOver">
                                            <Storyboard>
                                                <DoubleAnimation Storyboard.TargetProperty="Opacity" To="1.0" Duration="0:0:0.15" />
                                            </Storyboard>
                                        </VisualState>
                                        <VisualState x:Name="Pressed">
                                            <Storyboard>
                                                <DoubleAnimation Storyboard.TargetProperty="Opacity" To="0.6" Duration="0:0:0.05" />
                                            </Storyboard>
                                        </VisualState>
                                    </VisualStateGroup>
                                </VisualStateManager.VisualStateGroups>

                            </Border>
                        </Grid>
                    </DataTemplate>
                </GridView.ItemTemplate>

            </GridView>
        </Grid>
    </Grid>
</Page>